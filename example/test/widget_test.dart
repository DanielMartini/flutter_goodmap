import 'package:flutter_test/flutter_test.dart';
import 'package:example/main.dart';

void main() {
  test('example app is constructible', () {
    expect(const ExampleApp(), isA<ExampleApp>());
  });
}
